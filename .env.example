# Пример .env файла. Скопируй в .env и заполни.
# docker compose автоматически подхватит переменные.

# Домен или IP твоего VPS (для Nginx server_name)
DOMAIN=beer.example.com

# Порт Nginx (обычно 80)
NGINX_PORT=80

# Кол-во воркеров Gunicorn (обычно = кол-ву ядер CPU)
GUNICORN_WORKERS=4
